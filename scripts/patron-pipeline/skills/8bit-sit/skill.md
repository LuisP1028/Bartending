---
name: 8bit-sit
hostname: local-workspace
description: Transform the image from {/8bit-headon} into a centered front head-on seated bust in leaning-on-bar pose using the sit frame template. Always apply {/8bit-style} for stylization on solid tan background.
disable-model-invocation: true
---

- **CRITICAL ALIGNMENT:** Before executing any tasks, you must review and strictly adopt the system glossary defined in `@File:LANGUAGE.md`.
- Throughout this workflow, every assessment of `{errors}`, `{correctness}`, `{functionality}`, `{correct required outputs}`, `{sufficient}`, or `{insufficient}` MUST strictly adhere to the exact definitions and qualifiers established in that document.
- Do not rely on baseline assumptions. All sufficiency checks and state evaluations must be objectively measured against this explicit taxonomy.

Use two references in order:
(1) The image generated by `{/8bit-headon}` — this character must appear; preserve face, hair, clothing, identity, likeness and outfit.
(2) `assets/sitframetemplate.jpg` — the ONLY source of pose and camera framing (head-on seated bust silhouette with skeleton joints).

OUTPUT: Transform the `{/8bit-headon}` image into a CENTERED FRONT HEAD-ON seated bust (facing camera) in a leaning-on-bar pose: head to mid-torso, arms resting forward as if on a bar top. Large readable face. STRICTLY do NOT render any bar, bar top, table, counter, furniture, or surface under the arms — pose only, character floating on solid tan background with empty space where a bar would be. Copy the template's head-on seated framing and arm pose. DO NOT copy template colors: no black silhouette fill, no yellow joint dots, no green bone lines.

Apply stylization via `{/8bit-style}`.

HARD NEGATIVES: no side profile, no three-quarter profile, no full body, no legs, no standing pose, no bar props. Not a recolored side profile of the first reference.

Run a `{/sit}` session.