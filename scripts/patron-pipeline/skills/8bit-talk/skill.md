---
name: 8bit-talk
hostname: local-workspace
description: Open the mouth of the seated character from the {/8bit-sit} output for a clear talking expression while keeping pose, framing, clothing, lighting, and pixel style identical. Always apply {/8bit-style}.
disable-model-invocation: true
---

- **CRITICAL ALIGNMENT:** Before executing any tasks, you must review and strictly adopt the system glossary defined in `@File:LANGUAGE.md`.
- Throughout this workflow, every assessment of `{errors}`, `{correctness}`, `{functionality}`, `{correct required outputs}`, `{sufficient}`, or `{insufficient}` MUST strictly adhere to the exact definitions and qualifiers established in that document.
- Do not rely on baseline assumptions. All sufficiency checks and state evaluations must be objectively measured against this explicit taxonomy.

Use the image generated by `{/8bit-sit}` as the sole base reference.

Keep the EXACT sit pose, framing, clothing, lighting language, and pixel style identical to the `{/8bit-sit}` output. Only change the mouth to clearly OPEN for talking (visible mouth cavity / open lips). Same head-on seated bust. Do not change pose, angle, or background. Do not smile closed-mouth. Mouth must be open.

Apply stylization via `{/8bit-style}`.

Run a `{/8bit-talk}` session.