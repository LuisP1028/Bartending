# RE76

1. `GlassAsset.tsx`: Replace nested `<svg><use/></svg>` for ice/rim/garnishes with transform + `<use href>` (and xlinkHref) in the parent viewBox.
2. Shell CSS: keep vessel/art-root/svg overflow visible under playfield.
3. Verify GlobalSVGDefs ids still resolve.
