# RE71

1. `page.tsx` — shellFocusZoneId state; nav/open/close handlers; interactive control markup; hotspot focus class.
2. `gameboy-shell.css` — pointer-events on A/B/D-pad hit targets.
3. `globals.css` — `.hotspot-polygon--shell-focus` visual.
