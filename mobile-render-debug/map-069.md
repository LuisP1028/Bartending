# Map 069: Stretch desync vs cover re-sync

## Root cause

FS70 non-uniform stage fill + SVG meet desynced paths from art (partially fixed by preserveAspectRatio=none). Non-uniform stretch still risks feel/clip issues.

## Fix strategy

**Cover** stage (aspect 1184∶880) into playfield: `width/height = max(...)` so nest is full; crop stage edges if needed; art+SVG share uniform scale → co-registered; no black bands.

**Status:** CLEAR.
