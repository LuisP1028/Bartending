# Map 068: Black bands inside housing playfield

## Root cause

`.pov-stage` Pattern A **contain** (1184∶880) inside a taller housing nest leaves letterbox bands (black/dark).

## Fix

Shell-only override: stage **fills** playfield (`width/height: 100%`, no contain letterbox). Overflow hidden on playfield. Desktop CSS unchanged.

**Status:** CLEAR.
