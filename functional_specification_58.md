# Functional Specification 58 — Receipt scale, jigger touch floor, horizontal chrome compact

**Map:** [wayfinder/map-049.md](wayfinder/map-049.md)  
**Prior:** FS57 liquid silhouettes (keep). FS56 shell fill (keep).  
**Operator:** Fix all three residuals — receipts, jigger, horizontal.

## Objective

1. **Receipts** — On small stages, paper and receipt chrome scale must not collapse to unreadable toy size; summary remains structured (FS57 labels).  
2. **Jigger** — Touch-usable minimum larger than FS57 0.78 floor when stage is small.  
3. **Horizontal** — In landscape, mode strip + toolbar steal less vertical space so stage contain is as large as possible; Pages host chrome must not dominate usable height when using github.io.

## Non-goals

Stretch stage art; map-046 full scroll contract; recipe changes; liquid pipeline rewrite.

## Acceptance

| ID | Pass |
|----|------|
| A1 | Mobile portrait: receipt text clearly larger/more readable than pre-FS58 floor |
| A2 | Mobile: jigger trigger ≥ ~44–48px class hit target at min scale |
| A3 | Mobile landscape: more stage area; less empty logo/toolbar waste |
| A4 | Liquid bottles still silhouettes (FS57) |
| A5 | Desktop not absurdly oversized |
