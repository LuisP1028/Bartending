# FS74 — Auto-pan stage when carousel selected off-frame

On shell cover mode, when a carousel is **opened** (selected), if the carousel frame is clipped by the housing glass, pan the stage so the frame is fully visible. Do **not** pan on D-pad focus alone. Keep cover (no black bands). Reset pan when carousel closes.
