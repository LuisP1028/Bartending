# FS71 — Interactive Game Boy A / B / D-pad

Shell mode: D-pad moves focus among category hotspots; A opens focused carousel; B closes carousel. SELECT/START decorative. Touch unchanged. Desktop unshell’d unchanged.
