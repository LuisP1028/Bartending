# RE75

1. Compute glass-vs-stage insets after layout/pan (section vs stage getBoundingClientRect).
2. Set CSS vars on stage for HUD nudge; apply to `.jigger-pour-control` and `.receipt-hardware-cluster` under `.gb-shell__playfield`.
3. Update on shellStagePan, resize, openCategory (layout changes).
4. Free receipt papers do not use this nudge.
